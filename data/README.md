# version_2 data

External inputs used by the v2 framework.

Required files:
- `raw_stock_data.csv`
- `raw_macro_data.csv`
- `risk-free.csv`

These are copied from the current project inputs so `process` and `model` can run without reaching back into the old tree.
